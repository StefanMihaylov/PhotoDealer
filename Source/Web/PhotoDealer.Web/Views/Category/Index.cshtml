@using PhotoDealer.Web.ViewModels
@model  PictureViewModel

@using (@Html.BeginForm("Index", "Category", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @*@Html.ValidationSummary(true)*@

    @Html.DisplayFor(m => m.CategoryGroupId)

    @*@Html.ValidationMessageFor(m => m.CategoryGroup)*@

    <div>
        <input type="submit" class="btn btn-primary" />
    </div>
}

@*@Image.Create("", "");*@
